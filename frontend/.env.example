# URL del backend PocketBase.
# Solo necesario en desarrollo local (frontend en :3000, backend en :8090).
# En producción (Railway) no hace falta: la app usa la misma origen.
NEXT_PUBLIC_POCKETBASE_URL=http://localhost:8090
